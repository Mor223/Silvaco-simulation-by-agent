# Troubleshooting

This public package does not run simulators. Use it to analyze user-provided deck/log text only.

Static checks catch plotting commands, unavailable text-mode helper flags, unconfirmed refinement syntax, structure filename mismatches, and log blocks without solve commands.
